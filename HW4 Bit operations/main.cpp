#include <iostream>
#include <stdio.h>
#include <assert.h>
#include "tasks.h"


int main() {

    stepen(2);          // Степень записываем как аргумент
    bitvivod(10);       // Число для вывода записываем как аргумент
    onecounter(10);     // Положительное число вводим как аргумент
    upnumber(10);       // Положительное число вводим как аргумент
    return 0;
}